#ifndef BN_SOUND_ITEMS_H
#define BN_SOUND_ITEMS_H

#include "bn_sound_item.h"

namespace bn::sound_items
{
    constexpr inline sound_item confirm(0);
    constexpr inline sound_item deselect(1);
    constexpr inline sound_item excelent(2);
    constexpr inline sound_item good(3);
    constexpr inline sound_item great(4);
    constexpr inline sound_item ping(5);
    constexpr inline sound_item select(6);
}

#endif

